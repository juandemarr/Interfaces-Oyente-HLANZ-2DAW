cada seccion puede tener su header y footer, mientras que la etiqueta main solo aparece una sola vez.

li elemento de bloque, para definir ancho y alto
	a elemento de linea, no se puede definir lo de arriba

usar <abbr title="explicacion">, <acronym> aparecera el contenido del title en un cuadradito desplegable al pasar el raton

Especificación de HTML5 => https://html.spec.whatwg.org/multipage/

si en un enlace de <a> le indicamos al final #segundaseccion, no se irá al principio de esa pagina sino donde esté ese id

<sub>2</sub> subindice
<sup>2</sup> superindice